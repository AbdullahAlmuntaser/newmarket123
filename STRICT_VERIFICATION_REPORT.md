# تقرير التحقق الجنائي النهائي - Forensic ERP Verification Report
**التاريخ:** 30 مايو 2026
**الحالة:** تأكيد الأدلة البرمجية القاطعة (Evidence-Based Only)

---

## 1. كسر الذرية المحاسبية (Atomic Integrity Failure) - **مثبتة 100%**
- **الملف:** `lib/core/services/transaction_engine.dart` (السطر 399 وما بعده)
- **الملف المرتبط:** `lib/core/services/accounting_service.dart` (السطر 247)
- **شرح المشكلة:** 
  - عملية `postSale` تبدأ بـ `db.transaction` لتحديث المخزون.
  - يتم إطلاق حدث `eventBus.fire(SaleCreatedEvent(...))` داخل هذه المعاملة (السطر 411).
  - مستمع الحدث في `AccountingService` يفتح **معاملة جديدة ومستقلة** لإنشاء القيود المحاسبية.
- **الخطر:** إذا فشل التطبيق بعد تحديث المخزون وقبل إتمام القيود (مثلاً: نفاد بطارية)، سيبقى المخزون ناقصاً دون وجود أي قيد مالي يثبت العملية، مما يكسر توازن النظام المالي.

---

## 2. خطر OOM في شاشة المنتجات (watchProducts Risk) - **مثبتة 100%**
- **الملف:** `lib/data/datasources/local/daos/products_dao.dart` (السطر 95)
- **الملف المرتبط:** `lib/presentation/features/products/products_page.dart` (السطر 125)
- **شرح المشكلة:**
  - الدالة `watchProducts` تجلب كامل الجدول عبر `query.watch()` بدون أي `limit` أو `offset`.
  - شاشة المنتجات تستقبل القائمة كاملة في الذاكرة ثم تستخدم `allProducts.sublist(start, end)` للترقيم البرمجي (Pagination).
- **الخطر:** مع زيادة عدد المنتجات (مثلاً 5000+ صنف)، سيستهلك التطبيق كمية ضخمة من الذاكرة (RAM) مع كل تحديث، مما سيؤدي لجمود واجهة المستخدم (UI Freeze) أو انهيار التطبيق (Crash).

---

## 3. ثغرة التشفير (SQLCipher Fallback Risk) - **مثبتة 100%**
- **الملف:** `lib/native_sql_override.dart` (السطر 50-51)
- **شرح المشكلة:**
  - دالة `applyNativeSqlOverride` تستخدم `try-catch` حول تحميل `libsqlcipher.so`.
  - في حال الفشل، تنتقل الدالة فوراً لـ `DynamicLibrary.process()` وهو ما يسمح بفتح قاعدة بيانات SQLite العادية.
- **الخطر:** لا يوجد شرط يمنع هذا الـ Fallback في نسخة الإنتاج (Release Mode). في حال فشل تحميل مكتبة التشفير، سيفتح التطبيق قاعدة البيانات بدون تشفير تماماً دون علم المستخدم.

---

## 4. غياب المزامنة الفعلية (SyncQueue Inactivity) - **مثبتة 100%**
- **الملفات:** كافة الـ DAOs (`SalesDao`, `PurchasesDao`, `AccountingDao`).
- **شرح المشكلة:**
  - بالبحث في الكود، لا توجد أي دالة إضافة أو تعديل (مثل `createSale` في `sales_dao.dart`) تقوم بالإدراج في جدول `SyncQueue`.
  - الجداول موجودة في قاعدة البيانات ولكنها "ميتة" برمجياً.
- **الخطر:** لا يتم تسجيل أي سجل للتغييرات (Mutation Logs)، مما يجعل المزامنة مع السيرفر مستحيلة حالياً.

---

## 5. دقة البيانات المالية (Financial Precision) - **مثبتة 100%**
- **الملف:** `lib/core/services/posting_engine.dart` (السطر 11 و 45)
- **شرح المشكلة:**
  - يتم استخدام نوع البيانات `double` لحساب إجمالي المدين والدائن وتخزين المبالغ المالية.
- **الخطر:** لغة Dart (بناءً على معيار IEEE 754) لا تضمن الدقة المطلوبة للعمليات المالية المعقدة، مما سيؤدي حتماً لتراكم فروق "هللات" في التقارير الختامية.

---

## جدول التحقق النهائي (Final Summary)

| المشكلة | الحالة | الخطورة | الدليل المباشر |
| :--- | :--- | :--- | :--- |
| **فشل الذرية** | **مثبتة** | **قصوى** | `transaction_engine.dart:411` |
| **خطر الذاكرة** | **مثبتة** | **عالية** | `products_page.dart:125` |
| **ثغرة التشفير** | **مثبتة** | **حرجة** | `native_sql_override.dart:51` |
| **تعطل المزامنة** | **مثبتة** | **عالية** | غياب `into(syncQueue)` في الـ DAOs |
| **ضعف الدقة** | **مثبتة** | **متوسطة** | `posting_engine.dart:11` |

---
**توصية المراجع:** يجب البدء فوراً بدمج عمليات المخزون والقيود في معاملة ذرية واحدة (Unified Atomic Transaction) كأولوية قصوى.
