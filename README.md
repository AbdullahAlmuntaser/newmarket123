# نظام إدارة الموارد (System Market ERP)

نظام ERP متكامل مبني على معايير "محاضرات الأونكس" لإدارة الشركات التجارية والمحلات.

## الميزات التقنية
- **إدارة المحاسبة:** دليل محاسبي شجري، قيود يومية، مراكز تكلفة، وتقارير مالية (ميزانية، أرباح وخسائر).
- **إدارة المخازن:** دعم تعدد الفروع والمستودعات، تتبع دفعات الأصناف، وتنبيهات حد الطلب.
- **إدارة المشتريات والمبيعات:** دعم الفواتير النقدية والآجلة، ربط آلي مع المخزون والمحاسبة، وتعدد مستويات التسعير.
- **الرقابة والأمان:** نظام `AuditLogs` لتوثيق كافة العمليات، وحماية الواجهات الحساسة عبر `PermissionGuard`.
- **الطباعة:** محرك إصدار سندات (PDF) آلي يدعم طباعة سندات الصرف والتحويل المخزني.
- **دعم الفروع:** إمكانية إدارة فروع متعددة مع ربطها بمستودعاتها وحساباتها المستقلة.

## التقنيات المستخدمة
- **Flutter:** واجهات المستخدم.
- **Drift:** قاعدة بيانات محلية (SQLite).
- **Bloc:** إدارة الحالة.
- **Printing:** توليد المستندات والمطبوعات.

## الهيكل الفني (Schema)
تم ضبط كافة الجداول (Products, Sales, Purchases, GLAccounts) لتشمل الحقول التحليلية والربط المحاسبي الآلي.
---
تم التحديث بواسطة Gemini CLI - 27/04/2026.

## آخر تحديثات التطوير

### التهيئة الأولى والأمان
- لم يعد النظام ينشئ حساباً افتراضياً بكلمة مرور ضعيفة مثل `admin / 123`.
- عند أول تشغيل وغياب المستخدمين، تعرض شاشة الدخول نموذج تهيئة لإنشاء أول مسؤول بكلمة مرور يحددها المستخدم.
- يتم تخزين كلمة المرور باستخدام BCrypt، ويتم رفض كلمات المرور الأقل من 8 أحرف.
- يتم زرع بيانات الصلاحيات والأدوار الأساسية تلقائياً عبر `seedSecurityData` لضمان وجود baseline أمني.

### الأدوار والصلاحيات
- **Admin:** وصول كامل، ويجتاز فحوص الصلاحيات المركزية.
- **Manager:** وصول تشغيلي للتقارير، المخزون، المشتريات، الموردين، المنتجات، الموارد البشرية والتصنيع.
- **Cashier:** وصول محدود للمبيعات، المرتجعات، العملاء ونقطة البيع.
- تتم حماية المسارات عبر `AccessGuard` وتتم حماية العمليات الحساسة عبر `PermissionService`.

### اللغة والإعدادات
- يدعم التطبيق العربية والإنجليزية عبر ملفات l10n.
- يمكن اختيار لغة التطبيق من إعدادات النظام، ويتم حفظها في `AppConfigTable` بالمفتاح `locale_code`.
- يستخدم `LocaleProvider` اللغة المحفوظة عند تهيئة التطبيق بدلاً من إجبار العربية دائماً.

### المحاسبة والترحيل
- يتم إنشاء فترات محاسبية شهرية للسنة الحالية تلقائياً عند التهيئة.
- يتحقق `PostingEngine` من أن القيود غير فارغة، غير صفرية، غير سالبة، ومتوازنة قبل الحفظ.
- يستخدم الترحيل تاريخ القيد للتحقق من الفترة المفتوحة عند استخدام `postEntry`.


## أدلة الاستخدام والتشغيل
- راجع `USER_GUIDE.md` لخطوات تشغيل النظام اليومية: أول تشغيل، تسجيل الدخول، تغيير اللغة، المبيعات، المشتريات، المخزون، المحاسبة، التقارير، والنسخ الاحتياطي.
- راجع `TESTING_GUIDE.md` لأوامر الاختبار المطلوبة قبل دمج أي تغييرات.
