# تقرير التدقيق الشامل للنظام المحاسبي و ERP (Comprehensive Forensic Audit Report)
**تاريخ التقرير:** 1 يونيو 2026
**إعداد:** Senior ERP Systems Auditor & Software Architect

---

## 1. ملخص تنفيذي (Executive Summary)
تم إجراء فحص فني ومحاسبي عميق لنظام `systemmarket`. أظهر الفحص أن النظام يمتلك بنية تحتية جيدة (Clean Architecture) ولكنه يحتوي على أخطاء برمجية ومنطقية "حرجة" تهدد سلامة البيانات المالية ودقة تقييم المخزون. هذا التقرير يفصل هذه المشاكل ويضع خارطة طريق للإصلاح.

---

## 2. المشاكل الحرجة والمنطقية (Critical & Logic Bugs)

### [A-01] خطأ جوهري في خوارزمية LIFO (تقييم المخزون)
*   **مسار الملف:** `lib/core/services/inventory_costing_service.dart`
*   **المشكلة:** دالة `_calculateLifoValuation` تقوم بترتيب الدفعات حسب تاريخ الإنشاء تصاعدياً (`a.createdAt.compareTo(b.createdAt)`). هذا هو منطق FIFO وليس LIFO.
*   **التأثير:** تقييم المخزون في التقارير المالية خاطئ تماماً، مما يؤدي لنتائج ضريبية وأرباح غير دقيقة.
*   **مستوى الخطورة:** **Critical**

### [A-02] فقدان الدقة المالية (Precision Loss)
*   **الموقع:** معظم الـ Services (مثل `inventory_costing_service.dart`, `sales_service.dart`).
*   **المشكلة:** استخدام `double` في جمع القيم المالية (مثل `totalValue += ...`). الـ `double` غير مناسب للعمليات المالية بسبب أخطاء التقريب (Floating-point errors).
*   **التأثير:** ظهور فروقات "هللات" في الميزانية العمومية وميزان المراجعة، مما يجعل القوائم المالية غير متوازنة.
*   **مستوى الخطورة:** **Critical**

### [A-03] ازدواجية وتناقض منطق الترحيل (Path Divergence)
*   **المشكلة:** وجود طريقتين مختلفتين للترحيل:
    1.  `TransactionEngine.postSale`: متطور ويدعم الـ Batches.
    2.  `SalesService.processInvoice`: مبسط ويفتقر للدقة المحاسبية.
*   **التأثير:** تضارب في حالة المخزون والقيود المحاسبية اعتماداً على الشاشة التي تم منها البيع.
*   **مستوى الخطورة:** **High**

---

## 3. تدقيق قاعدة البيانات (Database Schema Audit)

### [D-01] عدم اتساق أنواع الحقول المالية (Type Inconsistency)
*   **المشكلة:** تفاوت في استخدام `Decimal` و `RealColumn` (Float) بين الجداول المرتبطة (مثل `Sales` مقابل `SalesReturns`).
*   **التأثير:** فشل في العمليات الحسابية المتقاطعة (Cross-table calculations) وفساد محتمل للبيانات.
*   **مستوى الخطورة:** **High**

### [D-02] تهجير بيانات وهمي (Empty Migrations)
*   **المشكلة:** الدالة `_backfillHrUuidIdsSafe` فارغة ولا تقوم بعملية التحويل المطلوبة للمعرفات.
*   **التأثير:** انهيار النظام عند تحديث الإصدارات القديمة.
*   **مستوى الخطورة:** **High**

---

## 4. تدقيق واجهة المستخدم والتدويل (UI/UX & i18n)

### [U-01] نصوص برمجية صلبة (Hardcoded Strings)
*   **الموقع:** `lib/presentation/features/pos/pos_page.dart` و `lib/presentation/features/accounting/shifts_page.dart`.
*   **المشكلة:** كتابة النصوص بالعربية مباشرة داخل الكود بدلاً من استخدام ملفات `app_ar.arb`.
*   **التأثير:** النظام غير جاهز للعمل باللغة الإنجليزية أو أي لغة أخرى بشكل احترافي.
*   **مستوى الخطورة:** **Medium**

### [U-02] مشاكل الاستجابة في شاشة الـ POS
*   **المشكلة:** استخدام Breakpoints ثابتة تؤدي لتداخل العناصر على شاشات الأجهزة اللوحية (Tablets).
*   **مستوى الخطورة:** **Medium**

---

## 5. خطة الإصلاح المقترحة (Prioritized Action Plan)

### المرحلة الأولى: إصلاح النزاهة المالية (1-3 أيام)
1.  تعديل `InventoryCostingService` لتصحيح ترتيب LIFO.
2.  تحويل كافة متغيرات الـ `total` و `amount` في الـ Services من `double` إلى `Decimal`.
3.  توحيد أنواع الحقول في ملف `app_database.dart` لتستخدم `Decimal` عبر جميع الجداول المالية.

### المرحلة الثانية: توحيد المعمارية (3-5 أيام)
1.  دمج منطق `SalesService` داخل `TransactionEngine` ليكون هناك "مصدر واحد للحقيقة" (Single Source of Truth) للترحيل.
2.  تفعيل منطق التحويل لـ UUID في الـ Migration بشكل فعلي.

### المرحلة الثالثة: الاحترافية والتدويل (5-7 أيام)
1.  استخراج كافة النصوص الـ Hardcoded ونقلها إلى `app_ar.arb` و `app_en.arb`.
2.  إعادة بناء Layout شاشة الـ POS باستخدام `Flex` و `Expanded` لضمان الاستجابة الكاملة.

---

## 6. ملاحظات إضافية
يجب عدم ترحيل أي فواتير جديدة حتى يتم إصلاح مشكلة الدقة المالية (Decimal) ومشكلة تقييم المخزون (LIFO) لضمان عدم استمرار تراكم البيانات الخاطئة في قاعدة البيانات.

**-- نهاية التقرير --**
